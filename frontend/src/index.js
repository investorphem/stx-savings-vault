// frontend/src/index.js

import React from 'react';
import ReactDOM from 'react-dom/client';
import App from'./App';

const root  ReactDOM.createRoot(documentgetElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
