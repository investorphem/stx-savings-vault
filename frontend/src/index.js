// frontend/src/index.js

import React from 'react';
import ReactDOM fro 'react-dom/client'
import App from './App'
const root ReactDOM.createRoot(document.getEementByI'root'));
root.rende
  <React.StrictMode>
    <App /
  </React.StrictMode>
);